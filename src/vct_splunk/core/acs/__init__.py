"""Minimal, read-only Splunk Cloud ACS (Admin Config Service) support.

Cloud coverage is read-only until a real canary certifies it against a live
stack. The credential-free integration test compares the live public OpenAPI
to the operation declarations that the client actually uses.
"""
