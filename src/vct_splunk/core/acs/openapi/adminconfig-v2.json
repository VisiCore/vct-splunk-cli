{
  "openapi": "3.0.0",
  "info": {
    "title": "Splunk Cloud Admin Config Service (ACS) adminconfig/v2",
    "version": "v2-pinned-subset"
  },
  "x-note": "Hand-pinned subset of the read-only ACS paths this CLI uses. The full spec lives at https://docs.splunk.com/Documentation/SplunkCloud/latest/Config/ACSIntro -- this file pins the exact paths our client calls so a contract test fails if the two drift.",
  "servers": [{ "url": "https://admin.splunk.com/{stack}/adminconfig/v2" }],
  "paths": {
    "/indexes": { "get": { "summary": "List indexes." } },
    "/inputs/http-event-collectors": { "get": { "summary": "List HEC tokens." } },
    "/roles": { "get": { "summary": "List roles." } }
  }
}
