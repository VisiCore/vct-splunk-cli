# Keep a Changelog repeats the same section headings (Added, Changed, Fixed)
# under every version. Scope MD024's duplicate-heading check to siblings so
# those legitimate repeats across versions are allowed, while true duplicates
# within one section are still caught.
MD024:
  siblings_only: true
